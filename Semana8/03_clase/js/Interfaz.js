//Dibujar elementos en el DOM

const imprimirProductos = (arregloProductos) => {
    const contenido = document.getElementById("divProductos")

    let cardsContenido = ''  //guardar el html generado a partir de los datos

    arregloProductos.forEach(() => {
        
    });
}